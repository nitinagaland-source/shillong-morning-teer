rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The public website never connects directly to Firestore.
    // All reads/writes go through the secured Vercel backend using Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
